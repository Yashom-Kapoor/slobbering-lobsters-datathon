---
name: gsap-scrolltrigger
description: Use this skill when building scroll-driven animations with GSAP's ScrollTrigger plugin. Covers pinning elements, scrubbing animations to scroll position, snap points, horizontal scroll sections, parallax effects, start/end markers and trigger configuration, batch animations for repeated elements, and performance considerations like will-change and refresh handling. Trigger on any mention of scroll-linked animation, sticky/pinned sections, scroll progress, or "animate as the user scrolls."
---